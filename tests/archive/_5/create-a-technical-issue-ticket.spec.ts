// spec: specs/alloy-portal-test-plan.md
// plan-id: ALLOY-PORTAL-TICKET-003
// seed: tests/seed.spec.ts

import { test } from "@playwright/test";

test.describe("Ticket Creation", () => {
  test("Create a technical issue ticket", async () => {
    test.fixme(
      true,
      "This scenario creates persistent demo data and needs final field selectors plus a cleanup strategy before it runs in the default suite.",
    );
  });
});
